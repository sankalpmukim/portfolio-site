---
export interface Props {
  class?: string;
}

const { class: className, ...props } = Astro.props;
---

<p class={`relative z-10 mt-2 text-sm text-zinc-600 dark:text-zinc-400 ${className || ''}`} {...props}>
  <slot />
</p>